<?php
// $Id$

/**
 * Implements hook_uninstall().
 */
function search_api_db_uninstall() {
  if (module_exists('search_api')) {
    $servers = entity_load('search_api_server', FALSE, array('class' => 'search_api_db_service'));
    foreach ($servers as $server) {
      search_api_server_delete($server);
    }
  }
}
